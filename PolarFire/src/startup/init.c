void system_init(void)
{
    /* Empty for now.
       Later add:
       - UART clock/reset
       - timer init
       - interrupt controller init
       - cache config
       - PMP setup if needed
    */
}
